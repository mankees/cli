#!/usr/bin/env node
var VERSION = require('../package.json').version;

var program = require('commander');

var mankee = require('../');


main();

function main() {
    var mankees = []; // TODO: parse mankee scripts from .mankee
    var args;

    program.
        version(VERSION).
        option('-l --list', 'list', listMankees.bind(undefined, mankees)).
        option('-s --silent', 'silent').
        parse(process.argv);

    args = program.args;

    // TODO: execute args in given order
}

function listMankees(mankees) {
    console.log('should list mankees now');
}
